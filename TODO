Release 0.1:

* Only show resolved variables (-s, --show-vars)
* Refactor API: from API entries to filters
  example: '{{ color_hex_to_rgb(color) }}' -> '{{ color|hex_to_rgb }}'
  New filters:
    some_value|bool_to_str(capitalized=True)
